<main class="container">
  <%= link_to sanitize("&larr; Back to home"), root_path %>
  <% if Playable.where(game: @game, user: current_user).count == 0 %>
      
      <%= render "form" %>
  <% end %>
</main>